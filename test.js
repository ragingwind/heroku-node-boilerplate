 /*global describe, it */
'use strict';

var assert = require('assert');

describe('Test your modules', function () {
  it('should return reasonable thins', function (done) {
    assert(false, 'You shoud make a test');
    done();
  });
})
